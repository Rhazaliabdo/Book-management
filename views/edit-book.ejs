<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/styles.css">
  <title>Ma Bibliothèque Magique - Éditer un Livre</title>
</head>
<body>
  <div class="container">
    <h1>Editer un Livre</h1>
    <form action="/edit/<%= book.id %>" method="post" enctype="multipart/form-data"> <!-- Ajout de l'attribut enctype -->
      <!-- Champ de titre -->
      <div class="form-group">
        <label for="title">Titre</label>
        <input type="text" id="title" name="title" class="form-control" value="<%= book.title %>" required>
      </div>
      <!-- Champ d'auteur -->
      <div class="form-group">
        <label for="author">Auteur</label>
        <input type="text" id="author" name="author" class="form-control" value="<%= book.author %>" required>
      </div>
      <!-- Champ d'ISBN -->
      <div class="form-group">
        <label for="isbn">ISBN</label>
        <input type="text" id="isbn" name="isbn" class="form-control" value="<%= book.isbn %>" required>
      </div>
      <!-- Champ de date de publication -->
      <div class="form-group">
        <label for="date">Date de publication</label>
        <input type="date" id="date" name="date" class="form-control" value="<%= book.date %>" required>
      </div>
      <!-- Champ de nombre de pages -->
      <div class="form-group">
        <label for="num_pages">Nombre de pages</label>
        <input type="number" id="num_pages" name="num_pages" class="form-control" value="<%= book.num_pages %>" required>
      </div>
      <!-- Champ de téléchargement d'image de couverture -->
      <div class="form-group">
        <label for="cover_image">Image de Couverture</label>
        <input type="file" id="cover_image" name="cover_image" class="form-control-file" accept="image/*">
      </div>
      <!-- Bouton de soumission -->
      <button type="submit" class="btn btn-primary">Enregistrer</button>
    </form>
    <a href="/" class="btn btn-secondary">Retour a la liste</a>
  </div>
</body>
</html>
